namespace bzTorrentClient.Avalonia.Features.TorrentList;

public enum TorrentSortMode
{
    /// <summary>The order torrents were added to the client.</summary>
    Default,
    Progress,
    Size,
    Name,
}
